.venv*/
.pytest_cache/
__pycache__/
*.pyc
*.egg-info/
.env
outputs/trainer/
adapters/epoch-*/
adapters/selected/
wandb/
runs/

# runs/ is excluded wholesale; commit a run's evidence with
#   git add -f runs/<name>/outputs
# and leave its adapters out (they ship as release assets).
